{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then echo 'DOCUMENTATION CHECK: Before committing, verify if README.md or CLAUDE.md need updates for: new features, changed commands, architecture changes, or new patterns. Only update if the changes warrant documentation.'; fi"
          }
        ]
      }
    ]
  }
}
